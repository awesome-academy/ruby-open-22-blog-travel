<% if params[:controller] == "blogs" || @destroy == "Blog" %>
  <%= link_to '<i class="fa fa-bookmark-o" aria-hidden="true"></i>'.html_safe, bookmarks_path(user_id: current_user.id, blog_id: blog.id, type: "Blog"), method: :post, remote: true %>
<% else %>
  <%= link_to '<i class="fa fa-bookmark-o fa-2x" aria-hidden="true"></i>'.html_safe, bookmarks_path(user_id: current_user.id, place_id: place.id, type: "Place"), method: :post, remote: true %>
<% end %>
